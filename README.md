# Mobile first development starter

Take a few moments to read through the HTML and CSS and make sure that you understand how it all works. Use VSCode Live Server to launch a local development server to serve the `index.html` file.

What changed:

* Updated the CSS to make image rows align at the top and keep text below the image group.

* index.html: 

* Fixed the first layout so the first image and its text are in a left column while the second image stays on the right.

* added div and article tag to group each image and text under image

* created a .two-column-row wrapper
.left-column: contains the first image plus the heading and paragraphs
.right-column: contains the second image

style.css: added .two-column-row with align-items: flex-start